meta {
  name: User login
  type: http
  seq: 1
}

post {
  url: {{baseUrl}}/api/auth/login
  body: json
  auth: inherit
}

body:json {
  {
    "username": "admin",
    "password": "admin123"
  }
}

vars:post-response {
  accessToken: "$.data.token"
}
